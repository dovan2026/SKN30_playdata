from __future__ import annotations



# health



# ready
@router.get(" ")
async def ready(request: Request) -> JSONResponse:
    settings =
    missing = 
    worker_ready = 
    ready_now = 

    return JSONResponse(
        status_code=


        content={
            
        },
    )

